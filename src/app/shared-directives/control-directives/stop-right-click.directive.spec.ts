/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StopRightClickDirective } from './stop-right-click.directive';

describe('Directive: StopRightClick', () => {
  it('should create an instance', () => {
    const directive = new StopRightClickDirective();
    expect(directive).toBeTruthy();
  });
});